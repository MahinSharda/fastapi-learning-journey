[project]
name = "fastapi-learning-journey"
version = "0.1.0"
description = "Learning FastAPI from scratch"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastapi[standard]>=0.139.1",
    "sqlalchemy>=2.0.51",
]